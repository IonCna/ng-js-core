import { NgModule } from "@/core/metadata/ng-module.ts";

/**
 * `LayoutModule` de `@angular/cdk/layout`: sus servicios (`MediaMatcher`, `BreakpointObserver`) se proveen solos en
 * la raíz, así que importarlo no es necesario — existe para que el código de Angular compile igual.
 */
@NgModule({})
export class LayoutModule {}
